function solucao(precos) 
{
    precos[100, 500, 100, 200,50]
    
    if(precos.length<5){
    soma=0;  
    for (i=0;i<precos.length;i++)
{ 
    soma=soma+precos[i]     
}
    console.log(soma)   //Output0    
}
    precos[100, 100, 100, 50]
    
    if(precos.length >= 5){
    function ordenaNum(a,b){
    return a-b;
}
    precos.sort(ordenaNum)
    precos.shift();
    soma=0;  
    for (i=0;i<precos.length;i++)
{ 
    soma=soma+precos[i]     
}
            console.log(soma)//Output1
}

}